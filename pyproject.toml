[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "snakesay"
version = "0.0.1"

[project.scripts]
snakey = "snakesay.__main__:default"
